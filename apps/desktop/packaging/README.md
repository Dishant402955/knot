# Branding assets for electron-builder (`directories.buildResources`).
#
# Add when ready:
#   icon.png   — at least 512×512 (builder derives .ico / .icns)
#   icon.ico   — Windows (optional if icon.png present)
#   icon.icns  — macOS (optional if icon.png present)
#
# Then set `win.icon` / `mac.icon` in electron-builder.yml if using custom filenames.
